" this is tuff from my old vimrc that I'm not yet sure whether I want after
" the spacevim switch. Leaving it here to remind myself to check.
set nowrap
set number
set laststatus=2
if version > 703
    set colorcolumn=80
endif
highlight OverLength ctermbg=red ctermfg=white guibg=#592929
match Overlength /\%81v.\+/
set title
set titleold=""

set autoindent
set tabstop=2
set expandtab
set shiftwidth=2  " this gets undone below, in my shift-operator mappings
filetype plugin indent on
